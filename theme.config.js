/** @type {const} */
const themeColors = {
  // ConvertTree Amazigh-inspired organic palette
  primary: { light: '#C85A17', dark: '#E8A76A' },      // Warm terracotta/orange
  background: { light: '#FFFBF7', dark: '#1A1410' },   // Warm off-white / deep brown
  surface: { light: '#F5EFE8', dark: '#2D2420' },      // Light beige / dark brown
  foreground: { light: '#3D2817', dark: '#F5EFE8' },   // Deep brown / light beige
  muted: { light: '#8B7355', dark: '#A89680' },        // Muted brown tones
  border: { light: '#E8D7C8', dark: '#4A3F35' },       // Warm borders
  success: { light: '#6BA83D', dark: '#8FBF4F' },      // Olive green
  warning: { light: '#D4A574', dark: '#E8C89A' },      // Golden tan
  error: { light: '#C85A17', dark: '#E8A76A' },        // Warm red-orange
};

module.exports = { themeColors };
